type Query {
  basicInformation: BasicInformation!
  node(id: ID!): Node
}

interface Node {
  id: ID!
}

type BasicInformation {
  id: ID!
  academicBackground: String!
  birthday: Date!
  gender: Gender!
}

enum Gender {
  FEMALE
  MALE
}

enum OrderDirection {
  ASC
  DESC
}
